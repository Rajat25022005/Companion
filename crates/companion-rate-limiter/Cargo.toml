[package]
name = "companion-rate-limiter"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "High-performance hierarchical rate limiter with Token Bucket, Sliding Window Log, and Leaky Bucket algorithms, tiered quotas, burst penalty cooldown, and Tokio concurrency benchmarks."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
chrono = { workspace = true }
parking_lot = "0.12"
dashmap = "6"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
